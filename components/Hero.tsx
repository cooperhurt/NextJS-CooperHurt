import headerStyles from "../styles/Hero.module.css";

const Hero = () => {
    return (
        <div>
            <p className={headerStyles.description}>
                This is a HeroImage
            </p>
        </div>
    );
};

export default Hero;
